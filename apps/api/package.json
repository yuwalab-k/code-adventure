{
  "name": "@code-adventure/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply code_adventure --local",
    "db:migrate:remote": "wrangler d1 migrations apply code_adventure --remote"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "wrangler": "^3.90.0",
    "drizzle-kit": "^0.28.0",
    "typescript": "^5.6.0",
    "@cloudflare/workers-types": "^4.20241004.0"
  }
}
